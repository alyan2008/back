---

-   name: Create AWS instance
    hosts: 127.0.0.1
    connection: local
    gather_facts: False
    remote_user: ubuntu
    roles:
        - install_aws_instance

#- hosts: testserver
#  roles:
#   - ansible-jira
